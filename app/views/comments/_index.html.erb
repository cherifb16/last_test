<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
      <!-- Addendum -->
        <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
      <!-- Jusqu'à présent -->
        <p>Comment : <%= comment.content %></p>
        <!-- Addendum -->
        <p>Customer : <%= comment.client.name %></p>
        <p><%= link_to "Edit", edit_order_comment_path(order, comment), remote: true %></p>
        <!-- Jusqu'à présent -->
        <p><%= link_to "Destroy", order_comment_path(comment.order_id, comment.id), method: :delete, remote: true,
                                        data: { confirm: "Are you sure ?"} %></p>
        <!-- Jusqu'à présent -->
      </li>
    <% end %>
  <% end %>
</ul>


